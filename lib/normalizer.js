'use strict';
module.exports = {
    normalizeName: function (name) {
        return name;
    },
    normalizeOptions: function (rawOptions, path) {
        console.log(path);
        return rawOptions;
    }
};